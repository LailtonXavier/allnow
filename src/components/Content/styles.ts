import styled from 'styled-components/native'

export const Container = styled.View`
  min-height: 200px;
  background: rgba(41, 46, 81, 1);
  box-shadow: 2px 1px 100px rgba(0, 0, 0, 1);
  border-radius: 44px;
  margin-top: 28px;
  padding: 18px;
  overflow-y: auto;
  z-index: 10;
`
